import axios from 'axios';
import { ENV } from '../../env'

function getListPrescription() {
    // const url = ENV.api_common + 'login';
    // return axios.post(url, params);
}

export { getListPrescription}